services:
  # Databases
  db:
    image: postgres:17.6
    restart: always
    environment:
      POSTGRES_PASSWORD: rahasiahehe
    volumes:
      - pgdata:/var/lib/postgresql/data

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
    depends_on:
      - db

volumes:
  pgdata: